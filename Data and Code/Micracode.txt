What was used in micracode.

Studying the Javascipt / NodeJs / React.

Settings; VS code, Ide (Integrated development Envirodment)
-compiter /interpreter, debugger
-refactoring (shoertering blocks of code ordenar)
includes terminal and file tree view.

Command line [ctrl alt T (unbuntuto enter)] no grafic interface to:
-install tools
-start a server
-source controll

Git, source controll recoverability and management, records on changes, to set up:
-git config --global user.name "Mona Lisa"
-git config --global user.email "email@example.com"

Shared in Github,

commands:

-Cd (change directory)
-ls (also ls ____ for content, not on windows)
-pwd (prints working directory)
-rm (also -r to remuve a folder)
-mkdir (make directory)
-code . (should open direct file)
-git log (log on activities)
-mv _what_   _on where_ (moving files)

Github:
-FORK a directory (copy)

Import local project:
-dont start with README
-Git init
-git add .
-git commit .m "text of the commit"
-copy the HTTPS 
-git remote add origin _copied adress_
-git remote -v (cheking destination)
-git push origin master

somre explenation.
-git init (not needed for copies, will create the hidden folder git)
-git add . (ALL files in the index, cache, Git Status to check colors)
-git coomit -m "__ " (setting the message while in local Repository)
-git remote add origin ___________ (setting the link)
-git remote -v (to chech the destination)
-git push -u origin master

Updating:
-git add . (ok so, if there is already a git hidden file)
-git commit -m "what will be said"
-git push origin master

Importing:
-Git Clone (after setting the route desired)

Vs code:
-crtl D column mode, repeat to get the same words.


-------------------------

Classes:
Espanol = 2 / English= 1

Es:
Aziz, chimico, sirio tarragona. online quarantena
Melina, u amigo le dijo, ni requrerda el nombre
Adriana, con el chico, 
Alfonzo, primer curso tibio, O face, de empresa.
Zaquiel, de correo, smith, 
jhoanna, reto personal , 
jorje, chimico.
Chris, russia, cambio sector.
nani, venez, curiosidad
jonh bravo, after quarantena.


ok so postSQL, database, 
Data, structured, store, retieve organize.
now we were doing with files, but now not only static but updates avaiable
also structured for different types of data.
like relational databases, key/value, graph databases, document database (NoSQL)
relational databases, Object-relational database system
scale data workloads, (Structured Query Language)
PostgreSQL is a RDBMS, Relational Database Management System.
Data stored in matrix, 2 dimensions arrays.

port number of of server, 
stored and modaled, TABLES.

crtl D to column mode to select specific words.

ok so 2 espanol
1 english

*39wZg8TLY*q